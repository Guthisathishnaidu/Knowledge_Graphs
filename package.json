{
  "scripts": {
    "frontend": "cd frontend && react-scripts start",
    "backend":  "cd backend && python -m uvicorn api:app --reload --port 8000",
    "dev":      "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.1"
  }
}